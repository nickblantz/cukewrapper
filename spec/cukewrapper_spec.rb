# frozen_string_literal: true

RSpec.describe Cukewrapper do
  it 'has a version number' do
    expect(Cukewrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
